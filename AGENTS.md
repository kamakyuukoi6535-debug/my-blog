# AGENTS.md — ブログ「昭和44年男」運用マニュアル

このリポジトリで作業するAIエージェント（Codex等）への指示書です。**作業前に必ず全部読むこと。**

---

## 1. このリポジトリ

| 項目 | 値 |
|---|---|
| ブログ名 | 昭和44年男 |
| 公開URL | https://showa44man.com/ |
| 生成 | Hugo v0.160.1 extended / テーマ PaperMod（gitサブモジュール） |
| ホスティング | Cloudflare Pages。`git push origin main` で自動デプロイ（1〜2分） |
| Amazonアソシエイトタグ | `showa44man22-22` |
| 記事数 | 約170本 |

### 書き手（記事の一人称「私」）

竹内一見。昭和44年（1969年）4月生まれ、57歳。東京都葛飾区高砂で生まれ育ち、現在も葛飾在住。
路線バスの運転士。前職は信用金庫。子ども5人。高校まで野球部。いまも筋トレとランニング。
FXで150万円を失った経験から、現在はS&P500・NASDAQ100のインデックス投資。

### 主力連載

「昭和の今日は何があった日？」— 昭和40〜64年の「今日」の出来事を、当時子どもだった目線でたどる日刊連載。

- ファイル名: `content/posts/showa-{英語の月}-{2桁の日}.md`（例 `showa-september-05.md`）
- 同じ日に2本目を出すときは末尾に `b`（例 `showa-august-25b.md`）
- 一覧ページ `/showa-calendar/` は `layouts/_default/showa-calendar.html` が自動生成する

---

## 2. 依頼される仕事

竹内さんは記事の下書き（front matter＋本文）を貼ってきて、こう言います。

> 「記事に合う画像数枚とふさわしいアフェリエイトを貼り付けてアップしてください」

やることは次の全部です。**途中で止めずに、公開して報告まで通しでやること。**

1. front matterを整える
2. 本文に色付け・マーカーを入れる
3. 画像を4〜6点用意する（Wikimedia Commons＋自作図解）
4. SNSカード画像（1200×630）を作る
5. Amazonアフィリエイトを2点、**実在確認してから**貼る
6. 内部リンクを3〜5本入れる
7. 前後の記事ナビを直す（前日の記事の「次の記事」も更新）
8. ローカルでビルドして、pushして、公開を確認する
9. 決まった形式で報告する

---

## 3. 絶対に守ること

これを外すと事故になります。過去に全部やらかしています。

1. **本文の文章を書き換えない。** 竹内さんの文章です。
   例外は「裏取りで判明した事実誤り」と「文中の明らかな矛盾」の2つだけ。
   直した場合は**必ず報告で申告する**。裏が取れないときは直さず、報告で「確認できませんでした」と言う。

2. **`date` を未来にしない。** 未来日時の記事は404になります。
   まず `date` コマンドで現在時刻を確認してから決めること。

3. **`date` は記事タイトルの日付に合わせる。**
   下書きの日付はほぼ毎回1日前になっています。タイトルが「9月5日」なら date も9月5日。
   ずれると記事一覧の日付バッジとタイトルが食い違います（過去に37本まとめて修正した事故あり）。

4. **`description` に半角の `"` を入れない。** YAMLが壊れて404になります。かぎ括弧は全角「」『』を使う。

5. **新しいタグを作らない。** 下記の正規セット30個から3〜4個選ぶ。

6. **push の前に必ず `hugo` でローカルビルドする。** テンプレートのエラーでサイト全体が壊れかけた事故あり。

7. **AmazonのASINを記憶で書かない。** 必ず商品ページを開いて確認する。

8. **人物写真は同定を確認してから使う。** 別人を載せかけた事故あり。

9. **自作した画像は必ず自分の目で見て確認する。** 文字のはみ出し・行の重なりが頻発します。

10. **公開URLは、聞かれる前に単独のコードブロックで出す。**

---

## 4. 手順

### 4-1. front matter

```yaml
---
title: "9月5日 ── 見出し｜昭和の今日は何があった日？"
date: 2026-09-05T05:00:00+09:00
draft: false
categories: ["昭和の今日は何があった日？"]
tags: ["昭和", "昭和44年男", "テレビ", "葛飾"]
description: "（120字前後。何が起きた日か＋竹内さん個人の視点）"
images: ["/images/september05-ogp.jpg"]
---
```

**タグの正規セット（この30個以外は使わない）**

```
昭和 / 昭和44年男 / 昭和の暮らし / 葛飾 / テレビ / スポーツ / インデックス投資 /
音楽 / 学校と子ども時代 / プロ野球 / お金と僕の12年戦争 / 高校野球 / 資産形成 /
家計管理 / 駄菓子 / 漫画 / 書評 / リベ大 / おもちゃ・ゲーム / 鉄道 / 宇宙 /
大相撲 / 不動産 / 固定費削減 / 映画 / 特撮・ヒーロー / オリンピック / FX / 奨学金 / NISA
```

**カテゴリ（この4つのどれか1つ）**

```
昭和の今日は何があった日？ ／ お金と暮らし ／ 走って・ぶら下がって・考えた ／ 昭和の記憶
```

### 4-2. 色付け

`hugo.toml` で `unsafe = true` にしてあるので、本文に生HTMLを書けます。

| クラス | 用途 | 目安 |
|---|---|---|
| `txt-blue` | **見出し（`##`）には必ずこれ** | 全見出し |
| `hl-y` | 黄色マーカー。記事の核になる一文 | 5〜8か所 |
| `hl-p` | ピンク。気持ち・情感 | 2〜4か所 |
| `hl-b` | 青。気づき・結論めいた一文 | 3〜5か所 |
| `txt-red` | 深刻な事実、死・事故 | 必要なときだけ |
| `txt-orange` | 数字の強調 | 1〜3か所 |

```markdown
## <span class="txt-blue">見出し</span>

本文の<span class="hl-y">大事な一文</span>をこう囲む。
```

**死者が出た事故・災害の記事では、犠牲者数などにマーカーを使わないこと。**
日航機123便の記事では色を抑え、陰謀論の本もアフィリから外しました。

### 4-3. 画像

1記事あたり本文用4〜6点＋SNSカード1点。

**出どころ**

| 種類 | どこから |
|---|---|
| 実物の写真 | Wikimedia Commons（ライセンスをAPIで確認） |
| 竹内さん撮影 | `C:\Users\kamak\OneDrive\Desktop\Code作業\` に置かれる |
| 図解・年表・グラフ | 自作（Python + Pillow） |

**著作権のあるキャラクター・作品の絵は使わない。**
ドラえもんの記事でも本人の絵は使わず、小学館ビル・本屋の棚・自作年表で構成しました。

**Wikimedia Commonsの確認（必須）**

```
https://commons.wikimedia.org/w/api.php?action=query&format=json
  &titles=File:ファイル名.jpg&prop=imageinfo&iiprop=url|extmetadata|size&iiurlwidth=1280
```

- `LicenseShortName` が CC BY / CC BY-SA / CC0 / Public domain のいずれか
- `Artist` と `Credit` が**空欄でない**
- 人物写真は `ImageDescription` に人物名があり、出典（雑誌名・発行元）がはっきりしていること
- 同定できない人物は写真を使わず、ゆかりの品や場所で代替する

**キャプションの書き方**

```markdown
![説明。（2021年撮影／Photo: CC BY-SA 4.0, via Wikimedia Commons）](/images/xxx.jpg)
```

写真と本文がずれる場合は、ずれを正直にキャプションに書く。
（例：本文は高砂店だが写真は四つ木店 →「私が本を買っていたのは高砂店の3階でした」）

**自作図解**

- フォント: `C:/Windows/Fonts/YuGothB.ttc`（太字）/ `YuGothR.ttc`（細字）
- 幅1200px、背景 `(250,248,245)` 系、本文の事実だけで作る
- 数字を出すなら**出典を画像下部に明記**、概算なら「概数」と書く
- グラフは**1軸だけ**。2軸は作らない。系列1つなら凡例なし
- 色は1色系統の濃淡。虹色は使わない
- **作ったら必ず画像を開いて目で確認する**

### 4-4. SNSカード（OGP）

**1200×630固定。** `tools/make_ogp.py` で自動生成できます。

```bash
python tools/make_ogp.py showa-september-05
```

手で作る場合のレイアウト：左端14pxの金帯／上に金色のラベル／白の大見出し2〜3行／横線／説明2行／最下部にサイト名。
背景色はカテゴリ別（昭和シリーズ＝濃紺 `(26,36,56)`、お金と暮らし＝深緑 `(22,48,38)`）。

### 4-5. アフィリエイト

**2点。** 記事の流れの中の自然な位置に置く（末尾にまとめない）。

```markdown
{{< amazon title="商品名" note="著者／出版社。内容の説明。星4.5（123件）" url="https://www.amazon.co.jp/dp/{ASIN}?tag=showa44man22-22" >}}
```

**確認すること（記憶で書かない）**

1. 商品名
2. 価格
3. 在庫（「在庫あり」「残り◯点」）
4. 星の数とレビュー件数
5. **カートに入れるボタンがあるか** ← マーケットプレイス出品のみの商品は使わない

さらに、**商品説明や目次を読んで記事の記述と一致するか確かめる**と質が上がります。
（例：ドラえもん第4巻の目次に「第1話／のろいのカメラ」があることを確認してから貼った）

**既出チェック**

```bash
grep -h -o 'amazon title="[^"]*"' content/posts/*.md | sort -u
```

### 4-6. 内部リンク

1記事3〜5本。本文の自然な語句をリンクにする。リンク先が実在することを必ず確認。

```bash
# リンク先の候補を探す
grep -l 'キーワード' content/posts/*.md | while read f; do
  printf "%-26s " "$(basename $f)"; sed -n '2p' "$f" | cut -c8-70; done
```

「バスの運転士」「信用金庫」「高砂」「野球部」など、繰り返し出る語には必ず対応する過去記事があります。

### 4-7. 記事末尾（この形を守る）

```markdown
---

*【昭和の今日は何があった日？】昭和四十年から六十四年までの「今日」を、当時子どもだった私の目線でたどっています。*

*▶ [日付から探す：「昭和の今日は何があった日？」記事一覧](/showa-calendar/)*

<!-- showa-nav -->

---

*▼ 昭和の今日は何があった日？（前後の記事）*

◀ 前の記事：[【9月4日】…](/posts/showa-september-04/) ｜ 次の記事：（近日公開）
```

そして**前日の記事の「次の記事：（近日公開）」を今日の記事へのリンクに書き換える**こと。
下書きに未来の記事へのリンクが書かれていたら「（近日公開）」に直す。

### 4-8. 公開

```bash
python tools/publish.py showa-september-05
```

検査 → ビルド → commit → push → 公開待ち → 到達確認まで自動。検査で落ちたら公開しません。

手でやる場合：

```bash
hugo --quiet --gc                      # ここで落ちたら絶対にpushしない
git add content/posts/xxx.md static/images/...
git commit -m "feat: 9月5日 …の記事を公開"
git push origin main
# 200になるまでポーリング
for i in $(seq 1 40); do
  code=$(curl -s -o /dev/null -w "%{http_code}" "https://showa44man.com/posts/showa-september-05/")
  [ "$code" = "200" ] && break; sleep 15
done
```

### 4-9. 公開後の確認

```bash
python tools/check.py     # 全記事の健康診断
```

個別に見るなら：記事URL・全画像が200／`twitter:card` が `summary_large_image`／
`og:image` の**直後**に `og:image:width` が来ている／`/showa-calendar/` に載っている。

---

## 5. 報告の形式（必ずこの順で）

1. **公開URLだけを入れた単独のコードブロック**（マークダウンのリンクだけで済ませない）
2. 貼った画像の一覧（配置・出典・ライセンス）
3. アフィリエイト2点（ASIN・価格・在庫・星・確認した内容）
4. 裏取りの結果／本文に手を入れた箇所
5. **X投稿文のコードブロック**（文字数を明記）

### X投稿文

- **全角2文字・半角1文字・URLは23文字**として数え、**280以内**
- 構成：`日付 ── 見出し` → 空行 → 事実1〜2文 → 空行 → 個人的な一言 → 空行 → ハッシュタグ → URL
- ハッシュタグは `#昭和44年男` ＋ 記事に関係するもの1つ
- URLは最終行に単独で置く

```bash
python tools/publish.py --count "投稿文"
```

---

## 6. 過去に起きた事故と対策

| 事故 | 対策 |
|---|---|
| 記事が404 | `date` が未来だった → 必ず過去時刻にする |
| 記事が404 | `description` に半角 `"` が入りYAMLが壊れた → 全角「」を使う |
| 日付バッジとタイトルがずれる（37本） | `date` をタイトルの日付に合わせる |
| サイト全体のビルドが壊れかけた | `{{ .Key \| int }}` が "08" を8進数と解釈 → push前に必ずローカルビルド |
| アフィリンクが別商品に飛ぶ | ASINを記憶で書かない |
| 人物写真が別人 | Commonsのextmetadataで人物名と出典を確認 |
| Xでカード画像が出ない | OGP画像が縦長だった → 1200×630にする |
| OGPタグが読み飛ばされる | `og:image:width` が `og:image` より前にあった → 直後に置く |
| 画像の文字がはみ出す | 作ったら必ず目で見る |
| タグページが量産される | 新しいタグを作らない |

---

## 7. 触ってはいけないもの

- `themes/PaperMod/` — gitサブモジュール。直接編集しない。
  テーマの挙動を変えたいときは `layouts/` 側に同名ファイルを置いて上書きする
  （`layouts/partials/templates/opengraph.html` がその例）
- `public/` — Hugoの生成物
- `hugo.toml` の `baseURL` / `buildFuture`

---

## 8. 同梱のスクリプト

| ファイル | 用途 |
|---|---|
| `tools/publish.py` | 検査 → ビルド → push → 公開確認。`--dry-run` で公開せず検査だけ。`--count` でX文字数 |
| `tools/make_ogp.py` | SNSカード画像を生成。`--all` で不足分を一括、`--strict` で1200×630でないもの全部 |
| `tools/check.py` | 全記事の健康診断（未来日付・日付ずれ・description・OGPサイズ・タグ・リンク切れ・孤児記事） |

初回のみ `pip install pillow` が必要です。
